import type { SongLabel } from "./songs";

/**
 * SubmitHub landing page links per song.
 * Add a link here when a song goes live on streaming platforms.
 */
export const SUBMITHUB_LINKS: Partial<Record<SongLabel, string>> = {
  "de(A)d ins(I)de": "https://www.submithub.com/link/dranix-dead-inside-s",
};
